name: ServerChan Notification
on: [push]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send WeChat Notification
        uses: Jack-Chen0120/serverchan@main
        with:
          sckey: ${{ secrets.SCKEY }}
          text: "您的serverchan-test仓库有新推送"
